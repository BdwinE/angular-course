import { createReducer } from '@ngrx/store';

const initailState = 0;
//export const counterReducer = createReducer(initailState);

export function counterReducer(state = initailState) {
  //create your own reducer.
  return state;
}
